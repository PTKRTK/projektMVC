@model IEnumerable<projektMVC.Models.BookCopy>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutEmployer.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Book.BookID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ISBN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BookCopyReleaseYear)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Book.BookID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ISBN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BookCopyReleaseYear)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookCopyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookCopyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookCopyID })
        </td>
    </tr>
}

</table>
