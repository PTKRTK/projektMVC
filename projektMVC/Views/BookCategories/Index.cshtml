@model IEnumerable<projektMVC.Models.BookCategory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutEmployer.cshtml";
}

<h2>Kategorie Książek</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategoryTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CategoryTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookCategoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookCategoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookCategoryID })
        </td>
    </tr>
}

</table>
