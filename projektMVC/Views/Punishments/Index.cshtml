@model IEnumerable<projektMVC.Models.Punishment>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutEmployer.cshtml";
}

<h2>Kary</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Charge)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Charge)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PunishmentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PunishmentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PunishmentID })
        </td>
    </tr>
}

</table>
